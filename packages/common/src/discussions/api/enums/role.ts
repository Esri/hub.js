/**
 * Role types
 *
 * @export
 * @enum {string}
 */
export enum Role {
  READ = "read",
  WRITE = "write",
  READWRITE = "readWrite",
  MODERATE = "moderate",
  MANAGE = "manage",
  OWNER = "owner",
}
