/**
 * @export
 * @enum {string}
 */
export enum AclSubCategory {
  /** Only valid for category: `group` or `org` */
  ADMIN = "admin",
  /** Only valid for category: `group` or `org` */
  MEMBER = "member",
}
