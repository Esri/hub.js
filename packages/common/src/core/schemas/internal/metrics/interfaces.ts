export enum LAYOUTS {
  simple = "simple",
  informational = "informational",
}
export enum SCALE {
  small = "s",
  medium = "m",
  large = "l",
}
export enum UNIT_POSITIONS {
  before = "before",
  after = "after",
  below = "below",
}
export enum VISUAL_INTEREST {
  none = "none",
  sparkline = "sparkline",
  icon = "icon",
  logo = "logo",
}
export enum SOURCE {
  dynamic = "dynamic",
  static = "static",
}
