import { removeUnusedDependencies } from "../../src/layout";

describe("removeUnusedDependencies", function() {
  it("all cards in all row in section should should have dependencies extracted", function () {
    expect(removeUnusedDependencies(section)).toEqual()
  });

  it("getItemResources throwing error should rethrow it", function () { 

  })
});
